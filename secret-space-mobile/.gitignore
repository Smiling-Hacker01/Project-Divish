node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.DS_Store
.env
.env.local

# Expo prebuild output — `expo run:ios|android` and `expo prebuild` regenerate
# these from app.json, so we keep them out of source control. Remove these
# lines only if you need to hand-edit native code.
ios/
android/
